pasang alias ini dengan:

```
source <(curl -fsSL https://raw.githubusercontent.com/indrahulu/linux-aliases/refs/heads/master/install.sh)
```

Setelah itu, `git fta` akan tersedia sebagai alias untuk `git fetch --all`.
